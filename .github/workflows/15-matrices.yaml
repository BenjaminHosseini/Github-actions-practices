name: 15 - Working with Matrices

on: 
    workflow_dispatch:

jobs:
    backwards-copatibility:
        name: ${{ matrix.os }}-${{ matrix.node-version }}
        runs-on: ubuntu-latest
        strategy:
            matrix:
                node-version: ['21.x', '22.x', '23.x']
                os:
                    - ubuntu-latest
                    - windows-latest
        steps:
            - name: Setup node
              uses: actions/setup-node@6
              with: ${{ matrix.node-version }}
            - name: Perform some tests
              run: echo "Running tests on OS ${{ matrix.os }} an NodeJS ${{matrix.node-version }}"
